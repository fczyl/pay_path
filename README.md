# 付费路径分析（Streamlit）

## 在线使用（推荐）
- 打开部署后的网页
- 在左侧 **上传 Excel**（不上传默认会尝试读取同目录文件）

## 本地运行
```bash
pip install -r requirements.txt
streamlit run app.py
```

## 数据说明
- 该仓库不需要提交内部 Excel 数据
- 使用时通过页面上传即可

